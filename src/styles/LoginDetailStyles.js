
import React, { Component } from 'react';
import {
  StyleSheet,
  Dimensions,
} from 'react-native';

const LoginDetailStyles = StyleSheet.create({
    container: {
        padding: 30,
        marginTop: 65,
    },
    heading: {
        marginBottom: 20,
        fontSize: 18,
        color: "#656565"
    },
});

export {LoginDetailStyles};
